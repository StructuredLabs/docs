---
title: "Delete Block"
api: "DELETE https://api.structuredlabs.io/delete-block"
description: "This endpoint deletes an existing API block."
---

### Body

<ParamField query="apiToken" type="string" optional="false">
  The API token for authentication.
</ParamField>

<ParamField query="serviceId" type="string" optional="false">
  The identifier for the service.
</ParamField>

### Response

<ResponseField name="success" type="boolean">
  Indicates whether the call was successful. Returns `true` if successful,
  `false` if not.
</ResponseField>

<RequestExample>

```bash Example Request
# Example Request to delete an block
curl --location --request DELETE 'https://api.structuredlabs.io/delete-block' \
--header 'Content-Type: application/json' \
--header 'Authorization: Bearer {apiKey}' \
--data-raw '{
    "blockId": "blk_123456",
    "apiKey": "api_key_789abc"
}'
```

</RequestExample>

<ResponseExample>

```json Response
{
  "success": true
}
```

</ResponseExample>
